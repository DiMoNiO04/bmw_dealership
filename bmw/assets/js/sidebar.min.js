document.addEventListener('DOMContentLoaded', () => {
  if (document.querySelector('.sidebar__list')) {
    const e = document.querySelector('.sidebar__list'),
      t = document.querySelectorAll('.sidebar__item'),
      l = document.querySelectorAll('.model__cars-js'),
      d = document.querySelector('.error'),
      i = document.querySelectorAll('.model__title'),
      o = (e) => {
        d.innerHTML = !1 === e ? 'Авто данной модели нет в наличии!' : '';
      },
      s = (e) => {
        let t = !1;
        if ('all' === e)
          for (let e = 0; e < l.length; e += 1)
            (l[e].style.display = 'flex'), location.href.includes('admin') || (i[e].style.display = 'block'), (t = !0);
        else
          for (let d = 0; d < l.length; d += 1)
            l[d].getAttribute('model') !== e
              ? ((l[d].style.display = 'none'), location.href.includes('admin') || (i[d].style.display = 'none'))
              : ((l[d].style.display = 'flex'),
                location.href.includes('admin') || (i[d].style.display = 'block'),
                (t = !0));
        o(t);
      },
      r = (e) => t[e].getAttribute('model');
    e.addEventListener('click', (e) => {
      const { id: l } = e.target;
      for (let e = 0; e < t.length; e += 1) t[e].classList.remove('sidebar__item-active');
      t[l].classList.add('sidebar__item-active'), (model = r(l)), s(model);
    });
  }
});
